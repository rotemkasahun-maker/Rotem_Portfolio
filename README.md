# Rotem Portfolio v0.1

Static, minimal portfolio concept.

Key interaction:
- The narrative line closest to the vertical center of the viewport becomes larger and fully opaque.
- Lines immediately before/after stay visible but quieter.
- This supports guided reading without hiding context.
- `prefers-reduced-motion` is respected.

Open `index.html` locally to preview.
